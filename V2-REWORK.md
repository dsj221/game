# V2 经营与规划重做记录

## 本次实际实现

- 邻接：独立配置规则，按旋转后的完整占地边缘测距；公园、水景、噪音、商业、医疗、仓库和道路影响；排除停用设施。修复旧公式把失业当作加分以及公园效果符号错误。
- 放置：覆盖格可视化、占用/未解锁/材料/金币/等级校验，统计实际覆盖人口、邻居幸福增量、维护、工资及有条件的收入上限；原地旋转会检查占地冲突。
- 信息地图：幸福、商业、就业、食品、医疗、环境、道路、土地价值八种视图。幸福颜色来源于实际入住居民；点击住宅可查看解释因素。土地价值是服务指数，不是交易价格。
- 寻路：已解锁土地网格、建筑障碍、道路优先、建筑入口、到达状态、道路改变后的重算。生产效率考虑到岗；消费者到店才成交。路线与位置随居民保存，旧存档可缺省。
- 土地：南部林地、东岸草甸、北山原野、西部河谷分别有范围、价格和等级/人口门槛，地图显示范围及名称；区域整购和逐块购买都检查门槛。以已购地图格保存，不重复扣款。
- 等级：增加第五级，三级以上需幸福度至少70；新建筑最多三级，已有高等级建筑存档仍可读取。

## 主要文件

- `src/systems/buildingInfluence.ts`、`placement.ts`、`land.ts`
- `src/npcs/navigation.ts`、`travel.ts`、`People.tsx`
- `src/world/InfluenceOverlay.tsx`、`LandOverlay.tsx`、`World.tsx`
- `src/components/PlacementInfo.tsx`、`LandRegions.tsx`、`TownPanels.tsx`、`TownHUD.tsx`
- `src/game/TownSimulation.ts`、`actions.ts`、`src/systems/persistence.ts`

## 经济

收入来自实际消费和有余额住户的租金；支出包括维护、工资、店铺采购及玩家建造/扩地/升级。家庭汇款仍为原有规则：每居民每天24金币进入居民钱包，不直接进入玩家账户。商业受库存、价格、消费需求、覆盖范围、道路和到达条件限制。预览显示的是条件估计，不保证收入。

## 验证

- `npm run build`：通过。
- `npm test`：35项通过，包括原有28项与新增7项。
- `node tests/v2-planning-browser.mjs`：通过，覆盖地图切换、非法放置提示、旋转、区域门槛与购买、保存后刷新、手机横向溢出及浏览器异常。
- 含实际寻路的两天模拟：31次成交，金币2000→2058，面包链有产出。

## 验证边界

这份记录不代表原始36节长清单逐条均已验收。尚未进行300居民的浏览器性能压力测试；居民愿望目前仍沿用既有任务规则；经济日报仍主要覆盖经营现金流，建造和奖励并未全部分类纳入日报。旧建筑没有多方向素材时仍显示原图。居民到达建筑入口后隐藏，以表示进入建筑，不包含室内行为动画。
