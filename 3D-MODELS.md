# 建筑 3D 模型

全部 52 种可建造建筑使用 Three.js 实体几何，包括主世界、下界、末地和建筑图鉴。造型为暖色低多边形，包含屋顶、门窗、支架、货物等结构；不是将原画贴到平面上，也不是原画的逐像素复刻。

- 在地图上按住鼠标右键拖动，可以观察侧面和背面。
- 点击建筑，详情默认展示可拖动旋转的模型；“查看原画图标”保留原素材。
- 放置、移动和旋转使用相同模型，按存档中的占地尺寸缩放。没有 footprint 的旧建筑仍保留单格。
- 夜间门窗、灯笼和传送门发光；风车叶片随游戏速度转动，暂停后停止。
- 建造菜单保留原画缩略图，避免每张卡片创建额外的 WebGL 场景。

原图结构在 `src/buildings/referenceGeometry.ts` 中逐项建模，使用 `buildingIconKeys` 与原图建立对应关系；共用原图的建筑共用相同模型。`src/buildings/modelGeometry.ts` 合并普通结构和发光网格，并缓存几何；`src/buildings/Model.tsx` 负责渲染和风车动画。

打开 `/?model-review=1` 进入建筑对照室，左侧为原图，右侧为游戏使用的实体模型，可切换全部建筑、旋转和下载独立 GLB 文件。模型重建原图中的可见结构，未观察到的背面为延续设计；材质为顶点着色实体材质，仍不等同于原图手绘光影的逐像素复刻。对照室测试为 `node tests/model-reference-browser.mjs`。

验证：`npm test` 覆盖所有建筑的模型存在性、有限坐标、接地和水平边界；`npm run build` 检查生产构建。`tests/models-browser.mjs` 分批渲染全图鉴，检查实体点击、旋转边界、放置和移动端；测试地址通过 `MODEL_TEST_URL` 指定（默认 5176）。截图位于 `test-results/models-*.png`，测试使用独立浏览器存档。
